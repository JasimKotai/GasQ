import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const GasStationList = () => {
  return (
    <View>
      <Text>GasStationList</Text>
    </View>
  );
};

export default GasStationList;

const styles = StyleSheet.create({});
